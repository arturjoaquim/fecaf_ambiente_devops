apiVersion: v1
kind: Service
metadata:
  name: api-express-service
spec:
  selector:
    app: api-express
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer